// Code not generated by ChatGPT but functions and methods were searched for using ChatGPT
const mainContainer = document.querySelector("main");

let containerNum = 6;
function createShowRec () {
    let showContainer = document.createElement("div");
    showContainer.className = "showContainer";
    showContainer.id = `show-${containerNum++}`;
    showContainer.textContent = "This is another show.";

    mainContainer.appendChild(showContainer);
}

myButton.onclick = () => {
    createShowRec();
};